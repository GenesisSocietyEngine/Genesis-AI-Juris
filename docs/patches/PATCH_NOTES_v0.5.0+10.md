# Patch notes — v0.5.0+10

- makes the generated Case Library the production mobile entrypoint;
- displays multiple fictional matters from repository content;
- adds English/Russian data-driven case localization;
- preserves stable case and party IDs across translations;
- exposes case-to-scenario readiness without pretending outlines are playable;
- launches the existing Failed ERP demo only through its explicit adapter;
- adds a back-to-library action without changing gameplay transitions;
- retains all v0.5.0+9.2.1 lifecycle, hearing, cassation, and test-isolation fixes.
