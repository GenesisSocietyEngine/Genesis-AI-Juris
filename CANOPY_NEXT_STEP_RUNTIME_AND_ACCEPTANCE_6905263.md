# Canopy — завершить проверку интегрированного кандидата

Продолжай в той же задаче Codex, где создан `6905263f3fc98092eb900d1283570b9a64b26917`. Цель следующего запуска — поддерживаемый runtime, полный успешный test gate, доступная настоящая browser acceptance и переносимый итог. Повторное восстановление уже интегрированного Canopy не требуется.

1. **Сохрани checkpoint.** Проверь `codex/canopy-integration-2026-09-07`, полный HEAD и рабочее дерево. Разреши сокращённый `3f967d8` в полный executable SHA. Сохрани существующие коммиты, audit history и receipts. Если появился более поздний HEAD, сначала исследуй его; reset не выполняй. Если это новая среда и `6905263` отсутствует, восстанови именно финальный ZIP `GENESIS-Canopy-Final-Integration-6905263-2026-09-07.zip` с SHA-256 `1f1e96e613c1f053a1805f768cb22adb94141da93d2779c4069eeac0f52d4a7d`; не собирай заново работу из старых UI-пакетов. Чужой `/workspace/...` не является автоматически доступным файлом.

2. **Исправь выбор Node до следующего запуска тестов.** Зафиксируй `command -v node`, `node --version`, `npm --version` и `process.execPath`. Проверь штатные установленные runtime/version managers. В более раннем запуске существовал `/root/.nvm/versions/node/v22.22.2/bin/node`: проверь этот путь, но не предполагай, что он сохранился в новой среде. При наличии активируй соответствующий `bin` для всей текущей задачи и дочерних процессов. Иначе используй совместимый runtime согласно package/CI, удовлетворяющий `>=22.13.0`, либо установи его штатным локальным способом. Обычная локальная подготовка runtime входит в разрешённую работу; hosted environment не меняй. Если установка запрещена политикой или недоступна по сети, зафиксируй точную операцию и ошибку, без обходов и повторного запуска на Node 20.

   Проверь доступность `node:fs/promises.glob` и SQLite с требуемым проектом флагом до длительного build. При необходимости переустанови зависимости штатным CI-скриптом или `npm ci` из существующего lockfile под выбранным Node. Сохрани engines и lockfile; не удаляй SQLite coverage, не вводи polyfill или downgrade ради Node 20. Успешный `node --version` сам по себе не заменяет проверку runtime, который фактически использует npm/build.

3. **Выполни полный gate на фактическом clean HEAD.** Запусти точные `npm test` и `npm run lint` из web package. Без фильтров, отключения тестов и замены build прямым вызовом test runner. Если текущий HEAD — `6905263`, это SHA нового прогона; не маркируй его как запуск на `3f967d8`. Если исправляешь код, сначала зафиксируй новый candidate и проверь его. Сохрани commands/cwd, полный SHA, toolchain, UTC start/end, exit codes, counts, skipped/cancelled и hashes raw logs. Исторические 562/562 остаются отдельным evidence b1fa8c7. Ожидай восстановленный состав Canopy tests, но объясняй реальный count, не подгоняй его под 562. Неудачные Node 20 attempts сохраняются как историческая диагностика.

4. **После gate выполни доступные browser checks.** Проверь все штатные browser capabilities среды, включая поддерживаемый headless browser/controller. Две отсутствующие команды на PATH не доказывают отсутствия любого браузера. Проверь через обычный интерфейс Templates → My cases / Canopy, EN/RU, React actions, keyboard/focus и узкую ширину экрана. HTTP/SSR smoke checks учитывай отдельно. Полный source review → Studio compile/publication → Base/Upside/Hard stop/Downside → save/reopen → PDF/JSON/approval/stale journey выполняй при реально доступных изолированном storage, обычной авторизации и необходимых ролях. Не подменяй identity/session headers и не создавай обходы авторизации. При конкретном browser/auth blocker продолжай независимые проверки и подготовку результата. Device emulation не является реальным телефоном; agent-controlled sessions не являются независимой человеческой приёмкой.

5. **Сохрани границы публикации.** Missing `make_pr` не блокирует runtime, tests и browser work. Возможность вызвать инструмент и разрешение выполнить PR write — разные условия. Подготовь точные base/head/branch, diff и PR description; GitHub push/PR/merge и новые deployment/env/migration/access действия этой инструкцией не разрешаются. Прежнее разрешение на точный Sites `6a6d6f8` и один unpublished save сохраняется со своими условиями; новый integration SHA оно не покрывает. Не повторяй push без требуемой диагностики `git-receive-pack`. Production P0 остаётся открытым без новых доказательств.

6. **Отчитайся и передай весь итог.** Укажи установленный runtime, фактически проверенный полный SHA и новый test count; отдельно browser attempted/passed, HTTP checks, human/device/video counts и конкретные remaining blockers. Перечисли реальные внешние операции. После evidence-only commit различай tested SHA и final HEAD; новый HEAD не называй полностью протестированным автоматически. Создай внешний bundle после последнего evidence commit, с tip, равным всему final HEAD, и prerequisite `e025131d87e35d4364d542acc5c84b6097eb657b` либо другим явно проверенным доступным ancestor. Проверь восстановление tip и Git integrity в чистом репозитории с этим prerequisite. Bundle и его checksum/manifest сохраняй вне коммита, который они описывают; передай через поддерживаемые task artifacts.

Первый обязательный результат — успешный полный `npm test` на совместимом Node либо конкретное доказательство, почему штатный совместимый runtime невозможно получить. Не заканчивай повторным сообщением о несовместимости уже известного Node 20 без выполнения доступной подготовки. Новые функции и повторный source recovery в этот этап не добавляй.
