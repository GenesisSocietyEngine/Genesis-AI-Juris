# Codex / GitHub — Canopy: продолжить реализацию и отчитаться

## Цель

Продолжи подготовку коммерческой демонстрации Project Canopy в `GenesisSocietyEngine/Genesis-AI-Juris`. Нужен проверенный интегрированный кандидат и фактический отчёт. Доступная локальная работа продолжается независимо от блокера передачи в Sites. Новый большой milestone не открывай.

## 1. Границы действий

Разрешены чтение репозитория и доступных CI/evidence, изолированная локальная интеграция, исправления, локальная тестовая конфигурация и synthetic migration fixtures, обычные локальные browser checks, тесты, локальные коммиты и подготовка артефактов.

Эта инструкция не предоставляет нового разрешения на GitHub push, создание/изменение PR, merge, deployment, изменения environment/migrations в Sites или production, access/publisher grants или invitations. Подготовь конкретный diff, описание PR и результаты до запроса такого разрешения. Отчёт верни в ответе Codex; самостоятельно не публикуй комментарии и не отправляй сообщения оператору.

Прежнее разрешение остаётся действительным только для точного `6a6d6f874c9453d27ae737fc4316092856291e9e` → `refs/heads/main` изолированного Sites-репозитория и одного неопубликованного save. Оно не переносится автоматически на `b1fa8c7` или интеграционный commit. Повторное одобрение тех же прежних операций не запрашивай. Их выполнение по-прежнему зависит от требуемой серверной диагностики и проверки remote.

Используй только изолированные synthetic DB/storage. Не подключай тесты к production, не копируй его данные или credentials. Не меняй целевой проект в hosting-конфигурации ради снятия блокера.

## 2. Проверенные входные материалы

Исходники должны быть доступны через эти ZIP либо уже проверенные Git objects/refs. ZIP также содержат receipts и историю блокера. Вложения и sandbox-ссылки из ChatGPT не считаются автоматически доступными в GitHub Codex.

| Вход | SHA-256 |
| --- | --- |
| `GENESIS-JURIS-Canopy-V3-ChatGPT-6a6d6f8-2026-09-07.zip` | `288bc8dfcbc99dec5314e4553207504cbc9251fec1ba5248a22b010b84f1495b` |
| `GENESIS-JURIS-Canopy-Entry-Fix-b1fa8c7-2026-09-07.zip` | `e396eda7b68f78d0194491906d1338a8ca4ba9d43e38f69d9be7f47138c5c6b0` |

Исходный ZIP содержит bundle с 296 коммитами, неизменённый Sites tar.gz и историю блокера. ZIP поправок содержит incremental bundle двух новых UI-коммитов, `changes.patch`, screenshots и receipts; ему нужна база `6a6d6f8`.

| Идентичность | Полный SHA |
| --- | --- |
| Проверенная реализация Canopy | `53a2e0c27e4bef417eec481802147a8e62ea0e6a` |
| Подготовленный Sites commit, непосредственный потомок 53a2e0c | `6a6d6f874c9453d27ae737fc4316092856291e9e` |
| Новый экран входа | `56b72dfedaa492baf425b4d68292455533769a29` |
| Языковая разметка; итоговый локальный кандидат | `b1fa8c77461f4658123d1218904e9dd85675c842` |

`6a6d6f8` отличается от `53a2e0c` только project ID в `.openai/hosting.json`. Не переносить этот ID в канонический GitHub checkout.

Документационный `72545e1…` и поздний Windows checkpoint `18b4438…` — отдельные известные точки истории. Их наличие в импортированном bundle не предполагается. Не объявляй отсутствующие коммиты восстановленными по одному текстовому упоминанию.

На `b1fa8c7` выполнен отдельный полный `npm test`: 562 passed, 0 failed/cancelled/skipped/todo; завершение 7 сентября 2026, 06:31:05 UTC. Lint прошёл. Это историческое evidence данного SHA, а не результат новой GitHub-интеграции.

## 3. Установи baseline и интегрируй изменения

1. Прочитай применимые `AGENTS.md` и инструкции репозитория. Зафиксируй текущую ветку, полный HEAD, рабочее дерево, доступный base и toolchain. Не выводи credentials из remote/config/environment.
2. Сохрани пользовательские изменения и более позднюю историю. Используй отдельную локальную ветку/checkout; никаких reset, force, удаления истории или слепого отката к старому `main`.
3. Проверь SHA-256, внутренние manifests и prerequisites bundle. Безопасно распакуй архивы: нормализуй Windows-разделители, отклони traversal и небезопасные ссылки. Импортируй source bundle в отдельные локальные refs без изменения текущей ветки; проверь Git integrity, полные SHA и ancestry.
4. Сравни фактический GitHub baseline с `53a2e0c`. Если Canopy уже интегрирован, не дублируй его. Если отсутствует — перенеси необходимую реализацию до `53a2e0c` через проверенный общий ancestor, сохраняя более новые изменения. Не cherry-pick все 296 коммитов и не подменяй всё дерево старым архивом.
5. Проверь по ancestry и эквивалентному diff, не присутствуют ли уже обе UI-поправки. Интегрируй недостающее из `56b72df` → `b1fa8c7`, адаптируя конфликты к текущему коду и не выполняя повторный cherry-pick. Изменены только `app/organizations/OrganizationBoundary.tsx` и `organizations.module.css`. Сохрани server-provided return URL, organization isolation, немонтирование private children до разрешения организации, EN/RU и `lang={locale}`.
6. Проверь diff относительно выбранной GitHub базы. Hosting/project IDs и более поздние пользовательские изменения требуют отдельного внимания. Новый integration SHA неизбежно отличается от исторического b1fa8c7 — укажи его отдельно.

Если материалы отсутствуют, сначала проверь доступные локальные Git objects и доступные read-only refs. Выполни полезную диагностику имеющегося baseline, затем назови один конкретный недостающий вход. Не имитируй Canopy заново и не заявляй интеграцию без его исходников.

## 4. Сними доступные UI-блокеры

Сначала проверь ordinary signed-out path поддерживаемым браузером и способом запуска из репозитория: Templates → My cases / Canopy, экран входа, ссылка возврата, EN/RU, клавиатура, отсутствие горизонтального обрезания. Проверь также переход в Case Studio и работу клиентских действий.

Предыдущее превью показывало экран, но некоторые React-действия не срабатывали; ordinary sign-in вернул 404. Прямое открытие DOM-указанного Vite-модуля вернуло `ERR_BLOCKED_BY_CLIENT`. Причина неработающих действий и блокировка модуля именно при загрузке страницы не доказаны. Не объявляй это дефектом production или уже установленной причиной hydration failure.

В доступной среде GitHub собери воспроизводимые наблюдения: команда запуска, URL/route, действия, browser/version, console/network errors без credentials. Исправляй подтверждённый дефект приложения или поддерживаемой конфигурации. Не обходи браузерные блокировки, не подменяй сессии, токены или identity headers. Если браузера или поддерживаемой авторизации нет, зафиксируй точную ошибку и продолжи независимую интеграцию/проверки.

## 5. Первый полный journey, если prerequisites доступны

Сначала подтверди изоляцию runtime, обычную авторизацию и доступность требуемых ролей. Разрешение на обычную регистрацию двух synthetic accounts сохраняется; production-регистрация зависит от прежних P0 gates. Новые publisher/access grants этой инструкцией не разрешаются.

Пройди обычными UI-действиями:

- Organization → Featured / Canopy → свежая Base copy с pending reviews и без унаследованных approvals.
- Точный source anchor → явная проверка proposal → обычный Studio import/compile/publication → фактический запуск и привязка run.
- Upside → отдельный Hard stop с доказанным изменением только clearance → независимый Downside.
- Save → close/reopen того же Matter/run → PDF/JSON → проверка exact-output approval и stale boundary, если подходящая reviewer-сессия действительно доступна.

Сохрани семантику: Base — 300 signed, 600 indicated, capacity 480; Upside — D03 v2, D06 v2, D08 v2; Hard stop — failed D06 v3 при неизменной экономике Upside. Downside использует source baseline 480 и явно объявленные stress assumptions: demand 300, yield 85%, energy +25%, payback 3.43. Не переименовывай assumptions в evidence. Story dates не являются датами реального выполнения.

Короткий memorandum имеет собственные bytes/hash и статус approval; approval полного PDF к нему не наследуется. Не сбрасывай исторический Base и audit history. ERP fallback остаётся synthetic duplicate bank-import incident; Signature Crop — storyboard.

API harness, expected cards и PDF renders не являются UI journey. Две сессии под управлением одного агента могут подтвердить техническое разделение ролей, но не независимую человеческую приёмку. Репетиции, cold users, реальный телефон и видео учитывай только по фактическому выполнению.

## 6. Проверь итоговый код

После завершения executable changes зафиксируй локальный candidate commit. Из фактического web package выполни полный `npm test` без `test-name-pattern`, отрицательных фильтров и скрытых пропусков, затем lint и обязательные gates репозитория. Новые тесты добавляй для конкретных рисков поведения, а не для проверки наличия строк CSS/JSX. Полный PDF corpus повторяй при изменении PDF-контракта или обязательном gate.

Сохрани raw logs и structured receipt: полный SHA, команда/cwd, toolchain, UTC start/end, exit code, counts, skipped/cancelled, log hash и состояние дерева. Если код изменён после проверки, прежний результат к нему не приписывай. Поздний documentation-only commit отрази отдельно от проверенного executable candidate.

## 7. Sites — отдельный открытый блокер

Последняя read-only проверка 7 сентября в 08:50 UTC: `appgprj_6a9dcacf5efc819196d6188105cd2cf6`, 0 versions, live/preview URL null, owner-only access revision 1.

Ответственный — Sites source-repository operator. Нужны `git-receive-pack` logs для `appgrepo_34e0efcb48dc8191b1effd4aa3e81634` за 6 сентября:

- 21:10:26–21:11:49 UTC;
- 21:13:50–21:15:15 UTC.

Требуются backend body receipt, unpack/validation/hooks, ref-update и supported processing timeout. HTTP 200 предварительного четырёхбайтового POST не является подтверждением большого push. Client upload-complete тоже не доказывает приём ref.

GitHub CI и Worker logs эти данные не заменяют. Не повторяй неизменённый push, не выдавай увеличение timeout или смену HTTP framing за установленное исправление. Если Sites tools отсутствуют, укажи это один раз; локальную работу продолжай.

## 8. Обязательный отчёт и передача результата

Верни отчёт со следующими пунктами:

1. **Что сделано:** реализация и наблюдаемое поведение, изменённые файлы, сохранённые пользовательские изменения.
2. **Идентичность:** исходная GitHub база, импортированные refs, integration branch, точный tested SHA, итоговый HEAD и различие между ними.
3. **Свежая проверка:** команды, результаты, время, ссылки на logs/receipts. Исторические 562/562 — отдельно.
4. **UI:** attempted/passed journeys, фактические шаги, screenshots, console errors, конкретные блокеры. Не писать «browser verified» без границ проверки.
5. **Hosting/production и люди:** что проверено сейчас, что только известно из прежнего отчёта, что не проверено. Никаких выдуманных reviewer/cold-user/video evidence.
6. **Следующие действия:** для каждого открытого блокера — ответственный, точный вход или операция и критерий снятия.
7. **External mutations:** перечисли фактически выполненные действия; если их не было — явно укажи 0 push / 0 PR writes / 0 merge / 0 save / 0 deploy.

Подготовь также читаемый diff/PR description и переносимый Git bundle новых коммитов с точным prerequisite и SHA-256, чтобы результат не остался только в недоступном sandbox. Передай их через поддерживаемый output задачи; при отсутствии такого канала назови ограничение. Не подменяй сохранение результата неразрешённым push.

Завершение этого запуска: интеграционный кандидат и доступные проверки реально выполнены, результат доступен для переноса, оставшиеся acceptance gates описаны доказательно. Коммерческая live-демонстрация не объявляется готовой по одним зелёным тестам.
